# Dependencies
node_modules/

# Build output
dist/
build/
*.log
*.tsbuildinfo

# Environment
.env
.env.*
.envrc
*.local

# OS
Thumbs.db
.DS_Store

# Python
__pycache__/
*.pyc
venv/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage/

# Database files
backend/data/*.db
backend/data/*.db-journal
backend/data/*.db-wal
backend/data/*.db-shm
backend/data/uploads/

# Sensitive backend config (track config.example.yaml only)
backend/config.yaml

# Editor
.vscode/
.idea/

# Service PIDs (runtime file)
scripts/.service-pids.json

# Playwright test results
frontend/test-results/
frontend/playwright-report/
frontend/playwright/.cache/
frontend/node_modules/.tmp/

# ----------------------------------------------------------------------------
# long-dev framework
# Framework definition files (agent / skills / commands / plugins / .ai docs)
# ARE part of the product and MUST be version-controlled — otherwise the
# Session Start Protocol and Context Switch commit steps are no-ops across
# machines/clone.
# Only runtime/cache state is ignored.
# ----------------------------------------------------------------------------
.opencode/node_modules/
.ai/cache/
.claude/
