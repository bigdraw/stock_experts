1、普通用户不应该关心数据获取，这几个主动触发数据获取的应该放到管理员用户那里；
2、后台服务启动后，自己按规则决定是不是要做全量更新，行情信息是在全量更新后的基础上每天增量更新；财务报表，每个月底去尝试拉取一次；自己比对当前时间和数据的上次拉取时间来决策；
3、各类指标计算/获取，做成skill或者mcp，方便agent模块去调用得出结论；
4、各个投资agent的设计，不仅可以从txt epub这类书籍里抽取，也可以用户填写文本框去自定义输入，或者填写一大段他认可的投资理念、公式，去构造。